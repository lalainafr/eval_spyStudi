{% extends 'base.html.twig' %}

{% block title %}- Formulaire création de PAYS
{% endblock %}

{% block body %}
	<div class="container">
		<section class="row justify-content-center">
			<h1 class='title text-center'>AJOUTER UN PAYS</h1>
			<br>
			{# Message flash #}
            {% for message in app.flashes('succes') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
			<br>
			<div class="col-md-4">
				{{ form_start(form) }}
				<br>
				{{ form_row(form.name) }}
				<br>
				{{ form_row(form.submit) }}
				{{ form_end(form) }}
			</div>
		</section>
	</div>
{% endblock %}
