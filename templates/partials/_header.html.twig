<div class="container my-5">
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<a class="navbar-brand" href="#">EVAL_SPY</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_home')}}">ACCUEIL</a>
				</li>

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_logout')}}">LOGOUT</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_login')}}">LOGIN</a>
					</li>
				{% endif %}

				{% if app.user %}
					{% for role in app.user.roles %}
						{% if role == 'ROLE_ADMIN' %}
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									Panel d'administration
								</a>
								<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">

									<li>
										<a class="dropdown-item" href="{{path('app_mission_list')}}">Mission</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_agent_list')}}">Agent</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_speciality_list')}}">Specialité</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_status_list')}}">Statut</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_country_list')}}">Pays</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_type_list')}}">Type</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_hideout_list')}}">Planque</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_target_list')}}">Cible</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{path('app_contact_list')}}">Contact</a>
									</li>
								</ul>
							</li>
						{% endif %}
					{% endfor %}
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
